resolver: lts-10.5

packages:
- .

docker:
  enable: true
  repo: "fpco/stack-build"

image:
  containers:
    -
      base: "dcastro/base"
      name: "dcastro/2048"
      entrypoints:
        - twenty48
      executables:
        - twenty48
      add:
        config/: /config
        static/: /static

